name: sepolia_ethereum_fireblocks
signing_provider:
  fireblocks:
    api_key: "${FIREBLOCKS_SANDBOX_API_KEY}"
    private_key_path: "/Users/josh/code/rrelayer/playground/sepolia_ethereum_fireblocks/fireblocks_secret.key"
    identity: "sepolia_ethereum_fireblocks"
    sandbox: true
    hidden_on_ui: true
networks:
  - name: sepolia_ethereum
    chain_id: 11155111
    provider_urls:
      - ${RPC}
    block_explorer_url: https://sepolia.etherscan.io
    enable_sending_blobs: true
    gas_bump_blocks_every:
      slow: 10
      medium: 5
      fast: 4
      super_fast: 2
api_config:
  port: 8000
  authentication_username: ${RRELAYER_AUTH_USERNAME}
  authentication_password: ${RRELAYER_AUTH_PASSWORD}
