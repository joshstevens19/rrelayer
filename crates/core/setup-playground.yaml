signing_key:
  raw:
    mnemonic: sight desk enter magnet sun belt wise vacuum cattle album chronic rack butter brass gaze box work oppose client win flight person excuse replace
  aws_secrets_manager:
    secret_name: 'rrelayer-server-signing-key'
    access_key_id: ${AWS_ACCESS_KEY_ID}
    access_key_secret: ${AWS_ACCESS_KEY_SECRET}
    session_token: ${AWS_SESSION_TOKEN}
    region: ${AWS_REGION}
networks:
  - name: base
    # seed: sight desk enter magnet sun belt wise vacuum cattle album chronic rack butter brass gaze box work oppose client win flight person excuse replace
    provider_urls:
      - https://base.gateway.tenderly.co
    #gas_provider: infura
#gas_providers:
#  infura:
#    - api_key: ${INFURA_API_KEY}
#      secret: ${INFURA_API_SECRET}
