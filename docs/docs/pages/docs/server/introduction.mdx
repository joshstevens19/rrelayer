# Server

The repo for rrelayerr is a monorepo to make it easier to maintain all so it has all the other elements in there, the server is location [here](https://github.com/joshstevens19/rrelayerr/tree/master/rrelayerr_server).

This will run a few things:

- The queue processes for the relayers
- The gas crons for the networks
- The API server for the relayers

Lets now look into how to get it setup.
