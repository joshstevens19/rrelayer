# HTTP requests

If you do not wish to use any of the SDK and call the methods directly via REST API requests you can do so details of the endpoints are [here](/docs/server/api/introduction).
All the SDK methods are just wrappers around these endpoints to make it easier to interact with rrelayerr.
